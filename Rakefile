# -*- ruby -*-

require 'rubygems'
require 'hoe'

Hoe.spec 'clunk' do
    developer('Indigo Casson', 'atamiser@gmail.com')

  # self.rubyforge_name = 'clunkx' # if different than 'clunk'
end

# vim: syntax=ruby
